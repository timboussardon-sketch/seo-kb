---
type: journal
title: Journal — 2026-05-21
date: 2026-05-21
tags: [journal, ia-employe, conversations]
status: auto-generated
sessions: 1
---

# Journal — 2026-05-21

> Compilé automatiquement depuis les transcripts Claude Code (seo-kb + organikk-next) sur 24h.
> 1 session de fond analysée (organikk-next, 02:46→03:18 UTC). La session seo-kb du jour est ce run `/recap-jour` lui-même — sans substance.

## 🎯 Sujets traités

- **Audit technique organikk.co — accessibilité** — traitement d'un rapport PageSpeed Google (score accessibilité 87) : ARIA, contraste, hiérarchie de titres. (organikk-next)
- **Audit technique organikk.co — performance LCP** — diagnostic et correction d'un render delay de 3 390 ms sur le hero de la home. (organikk-next)
- **Incident d'accès disque** — macOS (TCC) a coupé l'accès à `~/Documents` en pleine session, bloquant tout commit/push sur organikk-next. (organikk-next)

## 💡 Idées & hypothèses émises

- Le score Lighthouse lancé depuis DevTools n'est pas une référence fiable : il est pollué par les extensions du profil Chrome et la charge CPU de la machine au moment du run (origine : claude, en réponse à Tim).
- La seule note légitime et comparable dans le temps est PageSpeed Insights (pagespeed.web.dev) — environnement standardisé Google ; prendre la médiane de 3-4 runs car un run varie de ±5 points (origine : claude).
- Le vrai signal de ranking, ce sont les données terrain CrUX (Core Web Vitals réels sur 28 jours), pas le score lab — le lab ne sert qu'à diagnostiquer et itérer (origine : claude).

## ✅ Décisions prises

- **Menu mobile : `aria-hidden` remplacé par `inert`** — pourquoi : `aria-hidden="true"` laissait les liens dans l'ordre de tabulation ; `inert` retire vraiment le sous-arbre du focus clavier ET de l'arbre d'accessibilité, géré nativement par React 19. Ajout aussi d'un `aria-label` sur le dialog.
- **`role="img"` sur les spans d'étoiles** — pourquoi : un `<span>` sans rôle ne peut pas porter d'`aria-label` ; `role="img"` est le pattern correct pour une note en étoiles. Appliqué aussi aux composants `Stars` jumeaux de `/avis` et `/bootcamp`.
- **Resserrage du dégradé des titres de footer à `#2559DD`** — pourquoi : le dégradé descendait jusqu'à `#5b86f5` (~3,4:1), sous le seuil AA ; passages de plusieurs textes de `--muted-2`/`--blue-2` vers `--muted` et `#1B3E8F`.
- **Mode `immediate` ajouté au composant `Reveal`** — pourquoi : `.reveal { opacity:0 }` rendait le hero invisible jusqu'à l'hydratation JS, seule cause du LCP à 3,4 s ; le mode `immediate` rend `.reveal-now` (entrée 100% CSS via `@keyframes`), peint dès le premier rendu. Appliqué aux 3 blocs du hero ; le contenu sous la ligne de flottaison garde le `Reveal` au scroll.
- **Abandon de l'inlining CSS (`experimental.inlineCss`)** — pourquoi : faisait passer `index.html` de 24 à 80 KiB gzippé (CSS dupliqué `<style>` + payload RSC) et faisait perdre le cache CSS partagé entre les ~60 pages ; le gain de 150 ms ne le justifie pas. `next.config.ts` revenu à l'état initial.

## 🔄 Pivots & changements de cap

- Sur **l'inlining du CSS** : claude active `experimental.inlineCss` pour traiter l'insight « render-blocking requests » (150 ms), puis fait machine arrière dans la même session après avoir mesuré le triplement du HTML. Raison : duplication du CSS et perte du cache inter-pages ; un `<link>` render-blocking pour une feuille de styles reste normal et acceptable.

## ❓ Questions ouvertes

- Supprimer le doublon `src/data/strategies 2.ts` (fichier non suivi, hors commit) — laissé à Tim, à faire quand il veut.
- Contrôle visuel réel du hero (build + `npm run dev` local) — non fait par claude faute d'accès disque ; délégué à Tim.
- Relancer PageSpeed Insights après redéploiement pour confirmer la chute du render delay LCP — à faire une fois le site redéployé.
- Migrer `organikk-next` hors de `~/Documents` ? — TCC a coupé l'accès en pleine session, même cause qui avait fait déplacer le vault seo-kb vers `~/Code` le 2026-05-04. Le projet Organikk reste exposé au même blocage.

## 📦 Artefacts produits

**Modifiés** (organikk-next)
- `src/components/Nav.tsx` — `aria-hidden` → `inert` + `aria-label` sur le dialog du menu mobile.
- `src/components/Reveal.tsx` — réécrit : ajout de la prop `immediate` (entrée pilotée 100% CSS, sans dépendance au JS).
- `src/app/page.tsx` — corrections de contraste, hiérarchie de titres, et 3 blocs du hero passés en `immediate`.
- `src/app/avis/page.tsx` — `role="img"` sur le composant `Stars`.
- `src/app/bootcamp/page.tsx` — `role="img"` sur le composant `Stars`.
- `src/components/Footer.tsx` — contraste des titres de colonnes.
- `src/components/Footer.module.css` — resserrage du dégradé des titres de colonnes pour passer l'AA.
- `src/app/globals.css` — corrections de contraste + `.reveal-now` / `@keyframes revealNow` + `prefers-reduced-motion`.
- `next.config.ts` — `inlineCss` activé puis retiré (net zéro, retour à l'état initial).

**Commits** (organikk-next, poussés manuellement par Tim depuis son terminal)
- `1f12902` — Accessibilité : correctifs rapport PageSpeed (ARIA + contraste AA + hiérarchie de titres).
- `dec3aae` — Perf : LCP — hero visible dès le premier paint (fin du blocage `.reveal`).

---

## Liens vers la doctrine
- [[ia-employe]]

---

*Auto-généré via `/recap-jour`. Pour ajouter du contexte humain, édite à la main — le prochain run préservera tes annotations.*

---
type: journal
title: Journal — 2026-05-17
date: 2026-05-17
tags: [journal, ia-employe, conversations]
status: auto-generated
sessions: 3
---

# Journal — 2026-05-17

> Compilé automatiquement depuis les transcripts Claude Code (seo-kb + organikk-next) sur 24h.
> 3 sessions retenues : 1 organikk-next substantielle (17/05), 1 seo-kb avortée (17/05), 1 organikk-next du 16/05 finalisée trop tard pour le journal d'hier (rattrapée ici pour ne pas la perdre). Sessions ignorées : le `/recap-jour` qui a produit `2026-05-16.md` (méta) et le `/recap-jour` en cours.

## 🎯 Sujets traités

- **Typo site-wide Organikk — refonte du plancher** — Tim impose footer 15px PC / 14px mobile, tout le reste 18 PC / 17 mobile, capsule méta de blog jamais plus grosse que le corps, vérif page par page « JE N VEU AUCUNE ERREUR ». Abaissement du plancher de lisibilité, audit `!important`, build prod. (organikk-next)
- **Données structurées Organikk (JSON-LD)** — module unique `src/lib/schema.ts` : graphe Organization/ProfessionalService/WebSite/Person cross-lié par `@id`, émis site-wide depuis le layout, contenus référençant auteur/éditeur par `@id` + BreadcrumbList partout. (organikk-next, session 16/05 finalisée tard, hors capture du journal d'hier)
- **Revue de presse Algorithme du 17** — `/revue-presse-quotidienne` lancé ~01:07 UTC, aucune sortie produite dans le transcript local. (seo-kb, run avorté)

## 💡 Idées & hypothèses émises

- Un plancher typo unique via un sélecteur `:where()` à spécificité 0 + `!important` en fin de `globals.css` = source de vérité unique : il écrase tous les `style={{fontSize}}` inline sans avoir à toucher page par page, et rend inertes les ~60 `fontSize: 19` en dur qui traînent dans le source. (origine : claude, validé par Tim)
- Référencer auteur/éditeur par `@id` partagé au lieu de redéclarer l'entité à chaque page : un seul graphe d'entités, chaque contenu pointe dessus. (origine : claude)
- Les gros chiffres de stats (`+312%`, `.stat-n em`, `.k`) ne sont pas `aria-hidden` → normalisés au plancher, ils perdent leur effet d'impact. Exemptables en deux lignes. (origine : claude, à creuser)
- La lettrine décorative en début d'article (`::first-letter` ~96px) survit au-dessus du plancher : à aplatir aussi ? (origine : claude, à creuser)

## ✅ Décisions prises

- **Plancher de lisibilité abaissé 19/18 → 18px PC / 17px mobile, site-wide** — pourquoi : instruction Tim, lisibilité uniforme stricte, zéro petite typo nulle part.
- **Nouveau tier méta/footer à 15px PC / 14px mobile** — pourquoi : Tim veut le footer (mentions légales, ©) et les libellés méta/mono décoratifs (capsule ← Blog · catégorie · date · durée) sous le corps, jamais au-dessus.
- **Source de vérité = la règle `:where()` en fin de `globals.css`, pas la correction page par page** — pourquoi : garantir zéro dérive ; les `fontSize` en dur deviennent inertes.
- **Données structurées centralisées dans `src/lib/schema.ts`, graphe `@id` partagé** (16/05) — pourquoi : éviter de redéclarer l'entité auteur/éditeur sur chaque page.
- **Ne PAS exempter les gros chiffres ni inclure la nav** (16/05) — pourquoi : Tim a tranché « on ne touche à rien ».
- **`src/data/strategies 2.ts` (doublon Finder) volontairement non committé** — pourquoi : fichier parasite pré-existant en non-suivi, pas une modif de session.

## 🔄 Pivots & changements de cap

- Sur **le plancher typo** : le 16/05 on posait un plancher site-wide à **19px PC / 18px mobile**, et la barre méta de l'article était explicitement *laissée hors scope* (pill UI ~11px, non remontée). Le 17/05 on descend le plancher à **18/17** ET on crée un tier **15/14** qui force désormais cette même capsule méta sous le corps. Raison : instruction Tim plus stricte le 17 (« capsule plus grosse que le texte, cest une erreur »).
- Sur **la règle mémoire typo Organikk** : scopée « articles » → passée « site-wide » le 16/05, puis seuils mis à jour 18/17 + tier 15/14 le 17/05.

## ❓ Questions ouvertes

- Tim conteste « 6 fichiers ? Mais tu dois vérifier la taille de partout » et **interrompt la session** là-dessus : la preuve que le plancher `:where()` couvre tout (pas seulement les 6 fichiers committés) n'a pas été apportée. — à reprendre dès la prochaine session organikk : démontrer la couverture exhaustive avant de clore.
- Gros chiffres stats (`+312%`) normalisés → perte d'effet : exempter ou pas ? Laissé tel quel le 16, non rouvert le 17. — à reprendre si le rendu déplaît après déploiement.
- Lettrine `::first-letter` ~96px : l'aplatir au plancher aussi ? — non tranché, en attente d'un signal de Tim.
- Deploy prod : push effectués (`3bb2492`, `096dc50..317efa5`) → deploy déclenché par l'intégration GitHub de l'hébergeur, **non confirmable depuis Claude**. — à vérifier sur le dashboard Vercel.
- Édition Algorithme du 17/05 : run `/revue-presse-quotidienne` lancé mais aucune sortie locale. — vérifier si la GH Action a pris le relais, sinon édition manquante.
- `src/data/strategies 2.ts` : suppression propre proposée, non faite. — à trancher.

## 📦 Artefacts produits

**Créés**
- `organikk-next/src/lib/schema.ts` (16/05) — module unique : graphe JSON-LD Organization/ProfessionalService/WebSite/Person cross-lié par `@id`, émis site-wide.

**Modifiés** (organikk-next, 17/05)
- `src/app/globals.css` — plancher abaissé 19/18 → 18/17 + tier méta/mono 15/14, h5/h6 alignés à 18.
- `src/components/Footer.module.css` — règle dédiée footer (mentions légales, ©) à 15/14.
- `src/app/blog/[slug]/page.tsx`, `src/app/strategies/[slug]/page.tsx` — base `.article-prose` nettoyée.
- `src/components/Accompagnement30Form.tsx`, `src/components/QualityRaterQA.tsx` — correction des `font-size !important` qui échappaient au plancher.
- `~/.claude/projects/.../organikk-next/memory/{MEMORY.md, feedback_typo_articles.md}` — mémoire projet organikk : convention typo figée 18/17 + 15/14 (hors vault).

**Modifiés** (organikk-next, 16/05 — rattrapage)
- `src/app/layout.tsx`, `globals.css`, `blog/[slug]/page.tsx`, `blog/page.tsx`, `strategies/[slug]/page.tsx`, `newsletter/[slug]/page.tsx`, `actualites/[slug]/page.tsx`, `wiki/[slug]/page.tsx` — refs `@id` partagées + BreadcrumbList ; première version du plancher site-wide 19/18.

**Commits**
- organikk-next `3bb2492` (17/05) — Typo site-wide : plancher 18px PC / 17px mobile, footer + libellés méta/mono à 15/14 (6 fichiers).
- organikk-next (16/05) — Données structurées : graphe Organization/WebSite/Person partagé + BreadcrumbList ; puis push `096dc50..317efa5` (plancher typo site-wide 19/18).
- seo-kb `c8b7435` (17/05 09:42) — Dashboard 2026-05-17 (auto) — automatisation, pas de travail conversationnel.
- seo-kb `c4f98a1` (16/05 23:07) — Journal 2026-05-16 (auto) — sortie du `/recap-jour` précédent.

---

## Liens vers la doctrine
- [[ia-employe]]

---

*Auto-généré via `/recap-jour`. Pour ajouter du contexte humain, édite à la main — le prochain run préservera tes annotations.*

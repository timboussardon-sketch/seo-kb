---
type: journal
title: Journal — 2026-05-25
date: 2026-05-25
tags: [journal, ia-employe, conversations]
status: auto-generated
sessions: 2
---

# Journal — 2026-05-25

> Compilé automatiquement depuis les transcripts Claude Code sur 24h.
> 2 sessions analysées. Note : les deux sessions tournaient depuis cwd `/Users/timothee` mais ciblaient `~/Code/newFusionn` (artefacts journalisés dans `seo-kb/fusionn/Historique.md`). Le slash command pointe historiquement vers `seo-kb` + `organikk-next` — à étendre à `newFusionn` dans une prochaine itération.

## 🎯 Sujets traités

- **Audit complet Fusionn (5 passes)** — fonctionnel / UX / qualité LLM / conversion / dette technique, croisé avec les audits du vault. 21 chantiers proposés, 18 retenus. (newFusionn, journalisé seo-kb)
- **Phase 1 — bugs et nettoyage** — barème HN `/5 → /10` aligné (prompt + migration SQL + mocks), wording onglets vides ré-honnête, 3 edge functions / 1 page mortes supprimées, segmented control top-nav refactor `.map()`. (newFusionn)
- **Phase 2 — quotas et connexion** — bypass de la confirmation email Supabase, quotas freemium alignés `3 → 5/mois`, nouvel endpoint `get-all-quotas` (1 round-trip pour 4 compteurs), hook `useQuotas`, widget `QuotasFooter` dans la sidebar Stratégie. (newFusionn)
- **Phase 3 — Stripe portal et ResumeSection** — endpoint `create-billing-portal-session` qui résout le `stripe_customer_id` (plus de re-saisie email), section « Reprendre » extraite de l'historique et remontée en tête de compte en phase hero. (newFusionn)
- **Phase 4 — mobile et actions par vue** — `100vh → 100dvh` sur 3 layouts critiques (iOS Safari), composant `ViewActions` + 12 formatters markdown + JSON-LD FAQPage. (newFusionn)
- **Cards prix honnêtes (Landing + SubscriptionChoiceModal)** — audit du gating réel révèle que seules 2 limites sont vraiment appliquées (3 recherches + Export Excel). Refonte des 2 cards pour ne plus promettre du gating non implémenté. (newFusionn)
- **Pivot Phase 4.15 — 1 bouton « Copier le brief » global** — challenge de Tim « ça sert à quoi par vue ? » → 11 boutons retirés, FAQ conserve markdown + JSON-LD, ajout d'un bouton global qui agrège Structure Hn + Vecteurs + Micro-intentions + FAQ + Objections + Top 20 mots-clés. (newFusionn)
- **Fix entités HTML non décodées (`FusionnKnowsYouCard`)** — `&#x27;` qui apparaissait tel quel dans le bloc « Contexte de l'analyse ». Correction à la source dans `enrich-context/stripHtml()` + shield défensif au boundary front. (newFusionn)

## 💡 Idées & hypothèses émises

- Audits `Audit-prompts-vs-doctrine.md` et `Audit-ux-workspace.md` du vault sont périmés sur plusieurs points (faux positifs #4, #18, briefs Hn). À nettoyer lors d'un prochain refresh. (origine : claude, à creuser)
- Justifier le 20-29€/mois Premium passe par un vrai gating d'au moins une feature lourde (Agent IA, Plan d'action 3 mois, Stratégie pSEO). Aujourd'hui ces props `isPremium` sont déclarées mais jamais lues dans le render. (origine : claude, à creuser)
- Format markdown pipe-table comme standard de copy : compatible Notion, Obsidian, blogs, briefs. Choix validé pour tous les formatters de `copyHelpers.ts`. (origine : claude)
- Le mode `writer` dans `WorkspaceLayout` est du code mort depuis le 23 mai (aucun `setViewMode('writer')` dans l'UI). Candidat à suppression dans une prochaine session. (origine : claude, à creuser)

## ✅ Décisions prises

- **Retenir 18 chantiers sur 21** — pourquoi : Tim exclut `#8 ChatGPT/Perplexity dans Citation Probe`, `#9 footer outils + maillage inter-outils`, `#11 anti-hallucination [À SOURCER]` (hors scope ou non prioritaires).
- **Aligner quotas freemium sur 5/mois HN+Sémantique+GEO** — pourquoi : `types.ts` disait 3 mais `check-hn-score-limit` autorisait 5. Plus généreux et moins frustrant.
- **Cards prix 100 % honnêtes plutôt que d'ajouter du faux gating** — pourquoi : audit révèle que la quasi-totalité des features sont ouvertes en gratuit. Assumer la différenciation faible publiquement vaut mieux qu'une promesse non tenue.
- **1 bouton global « Copier le brief » remplace 12 boutons par vue** — pourquoi : sur 12 vues équipées, 1 seule (FAQ JSON-LD) avait un cas d'usage réel. Le besoin de Tim est d'agréger pour briefer, pas de copier par silo.
- **Commit groupé Phase 1+2+3+4 plutôt qu'une cascade de petits commits** — pourquoi : Tim assume la surface de risque (`c6db84e` empile 12 chantiers) ; les sessions futures seront unitaires si une régression remonte.
- **Migration HN `/5 → /10` doit aussi `DROP CONSTRAINT` + `ALTER COLUMN TYPE`** — pourquoi : check constraint historique `note_globale <= 5` bloquait l'UPDATE et `NUMERIC(3,2)` débordait au-dessus de 9.99 (`f557ed8`).
- **Edge function `enrich-context` corrigée à la source (vs front uniquement)** — pourquoi : ne pas laisser le bug ressurgir sur d'autres composants qui consommeront `siteFingerprint`. Shield front conservé en complément pour avant le deploy.

## 🔄 Pivots & changements de cap

- Sur **Phase 4.15 (actions « quoi en faire » par vue)** : avant on partait sur 12 boutons « Copier markdown » répartis sur chaque vue de résultats (`17a9bc7`). Maintenant on retire 11 boutons et on garde 1 bouton global « Copier le brief » dans la mode bar + 2 boutons spécifiques sur la FAQ (`d06b0a3`). Raison : challenge direct de Tim « ça sert à quoi par vue ? » → constat qu'aucun cas d'usage réel n'existait pour 11 des 12 vues.
- Sur **cards de prix Landing + SubscriptionChoiceModal** : avant la v1 (`8648a24`) listait des features promises sans audit du gating réel. Maintenant la v2 (`99909cb`) reflète exactement les 2 limites actives (3 recherches + Export Excel) et 2 différences cosmétiques (historique limité, stats cards masquées). Raison : Tim a signalé que la différence Freemium / Premium ne se voyait pas — audit lancé via agent Explore a confirmé que la promesse v1 était fausse.
- Sur **chantier #16 « Composant onglet unifié »** : avant l'audit du vault proposait de fusionner 3 modèles d'onglet actif identifiés. Maintenant on assume que ce sont 3 widgets distincts par nature (segmented control top-nav avec slider / sidebar Writer avec disabled+badge / sidebar Stratégie avec groupes+dot loading) et on les fusionnerait au prix d'une perte de cohérence. Refactor local uniquement (`.map()` sur le top-nav). Raison : le bénéfice théorique du DRY ne couvre pas la perte d'identité visuelle par contexte.

## ❓ Questions ouvertes

- Refactor `useCompteState` (#20 backlog) — à reprendre quand un nouveau bug d'état dans `/compte` ressurgit ou quand l'AgentChatView demande une nouvelle source de vérité.
- Unifier les 3 chats (#19 backlog) — à reprendre après décision sur le gating Premium (l'unification dépend de quels chats sont gated).
- Emails de retour (#6 backlog) — à reprendre quand Tim a une décision marketing sur le canal de réactivation.
- Surprise Gap dans 9 prompts (#13) et taxonomie Know-Simple/Know/Do dans 7 prompts (#12) — à reprendre en session dédiée prompts, pas dans une session UI.
- **Décision produit majeure** : gater vraiment Agent IA, Plan d'action 3 mois, Stratégie pSEO pour justifier 20-29€/mois Premium — à trancher avant la prochaine campagne d'acquisition.
- Audits périmés du vault (`Audit-prompts-vs-doctrine.md`, `Audit-ux-workspace.md`) — à nettoyer lors d'un prochain refresh d'audit, sans urgence.

## 📦 Artefacts produits

**Créés (newFusionn)**
- `src/components/workspace/ViewActions.tsx` — composant générique avec slots `getMarkdown` / `getJsonLd`, feedback « Copié » 1.5s.
- `src/lib/copyHelpers.ts` — 12 formatters markdown + `formatFaqJsonLd()` pour JSON-LD FAQPage.
- `src/hooks/useQuotas.ts` — hook qui appelle `get-all-quotas` (1 round-trip) et expose `{isPremium, quotas, resetsAt}`.
- `src/components/workspace/QuotasFooter.tsx` — widget compact en bas de la sidebar Stratégie, masqué si premium.
- `src/components/compte/ResumeSection.tsx` — section « Reprendre » réutilisable, montée en tête de `/compte` en phase hero.
- `supabase/functions/get-all-quotas/index.ts` — endpoint qui agrège 4 compteurs en parallèle (searches / hn / semantic / geo).
- `supabase/functions/create-billing-portal-session/index.ts` — endpoint Stripe portal sans re-saisie email.
- `supabase/migrations/20260525120000_rescale_hn_note_globale_to_10.sql` — migration `/5 → /10` avec `DROP CONSTRAINT` + `ALTER COLUMN TYPE` + double `UPDATE` conditionnel.

**Modifiés (seo-kb)**
- `fusionn/Historique.md` — 2 entrées du 2026-05-25 ajoutées (audit + Phase 1-4 + correctif cards prix + Phase 4.15 + fix entités HTML).

**Commits (newFusionn)**
- `d06b0a3` Pivot Phase 4.15 : 1 bouton « Copier le brief » global au lieu de 12 par vue
- `17a9bc7` Phase 4.15 v1 : Actions copier markdown sur 12 vues + JSON-LD FAQPage (corrigée par `d06b0a3`)
- `f557ed8` Fix migration HN : DROP CONSTRAINT et ALTER COLUMN TYPE manquants
- `99909cb` Cards prix honnêtes : reflet du gating réel de l'outil
- `8648a24` Cards prix : sync avec état réel de l'app au 25 mai (corrigée par `99909cb`)
- `c6db84e` Amélioration globale Phase 1+2+3+4 : bugs, quotas, rétention, mobile
- `b352cdd` Decode entities : titres et meta du site fingerprint propres

**Commits (seo-kb)** : aucun. `fusionn/Historique.md` reste en modification non commitée.

---

## Liens vers la doctrine

- [[ia-employe]]
- [[Audit-UI-design-system]] (à confronter — partiellement périmé)
- [[Audit-prompts-vs-doctrine]] (périmé sur #4 et briefs Hn — à refresh)
- [[Audit-ux-workspace]] (périmé sur #18 Tailwind `white` — à refresh)

---

*Auto-généré via `/recap-jour`. Pour ajouter du contexte humain, édite à la main — le prochain run préservera tes annotations.*

---
type: journal
title: Journal — 2026-06-21
date: 2026-06-21
tags: [journal, ia-employe, conversations]
status: auto-generated
sessions: 2
---

# Journal — 2026-06-21

> Compilé automatiquement depuis les transcripts Claude Code (seo-kb + organikk-next) sur 24h.
> 2 sessions substantielles analysées (1 grosse session bxble + automatisations seo-kb).

## 🎯 Sujets traités

- **Dashboard admin bxble** — construction d'un vrai dashboard de pilotage `/admin` branché sur Supabase, du néant à la prod sur bxble.com. Chantier dominant de la journée. (repo bxble, transcript logé sous organikk-next)
- **Réparation build bxble cassé par une autre automation** — restauration du RAG vault (`searchVault`) et auto-hébergement des fonts pour débloquer le déploiement Netlify. (repo bxble)
- **Automatisations vault seo-kb** — déclenchements crons du jour : revue-presse Algorithme, audit-vault-hygiene (erreur API socket), algorithme-recap-hebdo W25, recap-jour du 2026-06-20. (seo-kb)

## 💡 Idées & hypothèses émises

- Mode démo gated par `ADMIN_DEMO=1` pour prévisualiser l'UI du dashboard sans infra Supabase ni Docker, inerte en prod. (origine : claude)
- Sur Netlify serverless, le logging doit s'exécuter **avant** `controller.close()` du stream, sinon l'insert peut être gelé avant la fin de la requête. (origine : claude)
- Coexistence de deux automations Claude sur le repo bxble en parallèle : risque structurel de merges qui se cassent mutuellement, à surveiller. (origine : claude, à creuser)

## ✅ Décisions prises

- **Backend = Supabase, texte des conversations stocké en clair anonyme** — pourquoi : l'ancien `/admin` ne lisait que le localStorage local de Tim, aucune persistance serveur, donc rien à piloter.
- **Retirer toute l'auth admin en prod** — pourquoi : choix assumé de Tim malgré l'alerte (le dashboard expose des questions intimes des visiteurs ; `/admin` reste en `noindex`). Voir question ouverte sur l'exposition.
- **Restaurer le vault RAG plutôt que laisser le main cassé** — pourquoi : l'autre automation avait reverté `lib/rag.ts` en supprimant `searchVault` (la « voix Tim ») tout en gardant la route chat qui l'appelle ; build cassé sur origin/main indépendamment du dashboard.
- **Interdiction d'inventer des données** — pourquoi : Tim a tranché net (« JE VEUX LES VRAIES DONNÉES, interdit d'inventer ou de faire des démos ») ; le dashboard démarre à zéro et se remplit aux vraies visites.

## 🔄 Pivots & changements de cap

- Sur **les données du dashboard** : d'abord un dashboard peuplé de données démo réalistes (784 conversations, 8,66 € Gemini, etc.), puis pivot total exigé par Tim vers les vraies données Supabase uniquement. Raison : Tim refuse toute donnée fictive pour piloter.
- Sur **l'authentification admin** : construite d'abord (cookie HMAC signé, login serveur), puis entièrement démontée à la demande de Tim. Raison : il prend le risque d'exposition en connaissance de cause.

## ❓ Questions ouvertes

- Régénérer la clé `service_role` Supabase — à faire côté Tim ; des clés Supabase ont transité en clair dans le chat pendant le setup.
- `/admin` sans aucune auth en prod expose les questions sensibles des visiteurs (deuil, doutes, péché) à quiconque tape l'URL — à reprendre si Tim veut au moins une protection minimale.
- Coexistence avec l'autre automation sur `main` de bxble — comment éviter qu'elle re-casse le build ou écrase le vault au prochain merge ? À cadrer.
- Audit-vault-hygiene du jour a échoué (erreur API socket) — relancer.

## 📦 Artefacts produits

**Créés (repo bxble)**
- `app/admin/(dash)/` — 8 pages dashboard : vue d'ensemble, comptes, conversations, questions, trafic, plans, contenu, système.
- `app/admin/login/page.tsx` + `app/api/admin/{login,logout}/route.ts` + `lib/admin-auth.ts` — auth admin (créée puis retirée).
- `app/api/track/route.ts` + `components/Tracker.tsx` + `lib/anon.ts` — collecte d'événements anonymes.
- `app/api/account/route.ts`, `app/api/admin/classify/route.ts` — API comptes + classification de thèmes (Gemini Flash).
- `lib/admin-stats.ts`, `lib/admin-demo.ts`, `lib/supabase.ts`, `lib/pricing.ts` — couche data + mode démo + coût Gemini.
- `supabase/schema.sql`, `env.example`, `ADMIN-DASHBOARD.md` — schéma DB, env d'exemple, doc de setup.

**Modifiés (repo bxble)**
- `app/api/chat/route.ts` + `components/ChatInterface.tsx` — logging des conversations (sessionId/conversationId, déplacé avant close du stream).
- `lib/rag.ts` (restauré), `next.config.ts` (fusionné), `app/layout.tsx`/`globals.css`/`package.json` — réparation vault + fonts auto-hébergées.

**Commits (bxble)**
- `fded73f` — feat(admin): dashboard de pilotage bxble (Supabase)
- `a5386be` — fix(build): auto-héberge Geist (package geist) au lieu de Google Fonts
- `f8007bb` — fix(rag): restaure le vault (searchVault) cassé par le merge intertextualité

---

## Liens vers la doctrine
- [[ia-employe]]

---

*Auto-généré via `/recap-jour`. Pour ajouter du contexte humain, édite à la main — le prochain run préservera tes annotations.*
